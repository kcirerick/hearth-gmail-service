<!DOCTYPE html>
<html lang="en>
  <head>
    <meta charset="UTF-8">
    <title>Contacts</title>
  </head>
  <body>
    <h1>Contacts</h1>
    <ul>
      <% for(let i=0; i < contacts.length; i++) { %>
        <li><%= contacts[i] %></li>
      <% } %>
    </ul>
  </body>
</html>